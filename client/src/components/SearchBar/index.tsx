import React from "react";

type Props = {};

const SearchBar = (props: Props) => {
  return <input type="text" className="w-2/4 rounded-md shadow-md" placeholder="Search..."></input>;
};

export default SearchBar;
